<!-- users.ejs -->

<!DOCTYPE html>
<html lang="en">

<body>
  <h1>User Data</h1>
  <% for (let i = 0; i < userData.length; i++) { %>
    <p><%= userData[i].id %>. <%= userData[i].name %>, age: <%= userData[i].age %> </p>
  <% } %>
</body>
</html>
