{
  "dependencies": {
    "ejs": "^3.1.9",
    "express": "^4.18.2",
    "method-override": "^3.0.0",
    "react-router-dom": "^6.10.0"
  },
  "name": "anamo",
  "version": "1.0.0",
  "main": "index.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "This is a simple Node.js server that demonstrates basic file operations using Node.js's `fs` and `path` modules. The server provides two routes: a homepage (\"/\") and an API route to retrieve and add user data (\"/api/users\")."
}
